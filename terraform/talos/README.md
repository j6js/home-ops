# Talos Image for OCI
This folder would contain the `oracle-arm64.raw` image for Talos Linux, but due to it's large size, it is `.gitignore`'d.

To obtain the file, please use Talos Linux Image factory